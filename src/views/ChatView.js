import { Box, Typography } from '@mui/material';
import { motion } from 'framer-motion';

function ChatView() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Chat Console</Typography>
      {/* We'll add the chat implementation here */}
    </Box>
  );
}

export default ChatView;
