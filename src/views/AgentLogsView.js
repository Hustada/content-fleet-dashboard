import { Box, Typography } from '@mui/material';
import { motion } from 'framer-motion';

function AgentLogsView() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>Agent Logs</Typography>
      {/* We'll add the actual logs implementation here */}
    </Box>
  );
}

export default AgentLogsView;
